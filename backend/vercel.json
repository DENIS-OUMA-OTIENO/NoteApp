

{
  "builds": [
    {
      "src": "backend/index.ts",
      "use": "@vercel/node",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "backend/index.ts"
    }
  ]
}
